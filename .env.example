# Database — reads existing Cinderhaven marts
DATABASE_URL=postgresql://user:password@localhost:5432/cinderhaven

# App
APP_ENV=development
LOG_LEVEL=INFO

# Optional: override canonical data path for local dev
# CANONICAL_DATA_PATH=/path/to/marts
